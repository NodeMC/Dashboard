// HTTP
export const baseURL = "http://localhost:8081"; // Change to reflect the address and port of CORE

// Authentication
export const hawkAlgo = "sha256";

// User creation settings (may be moved to server)
export const anonymousCreateUser = true;
export const minUsernameLength = 4;
export const minPasswordLength = 8;
